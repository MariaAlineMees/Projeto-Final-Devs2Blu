server {
    listen 80;
    server_name localhost;

    # Caminho para os arquivos estáticos do Angular
    root /usr/share/nginx/html;
    index index.html index.htm;

    # Configuração para o Angular Router (trata o erro 404 de rotas)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Configuração do Proxy Reverso para a API
    # Redireciona todas as chamadas de /api/... para o serviço de back-end
    location /api/ {
        proxy_pass http://roteiro-service:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
